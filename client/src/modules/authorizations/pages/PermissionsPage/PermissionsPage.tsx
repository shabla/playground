import React from "react";
import {
  Page,
} from "@/components";

export const PermissionsPage: React.FC = () => {
  return (
    <Page
      title="Permissions"
      className="permissions-page"
      showNavbar={false}
    >
      permissions
    </Page>
  )
}